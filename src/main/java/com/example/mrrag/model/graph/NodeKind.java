package com.example.mrrag.model.graph;

/**
 * Kinds of nodes that can appear in the AST symbol graph.
 */
public enum NodeKind {
    CLASS,
    INTERFACE,
    ENUM,
    ANNOTATION_TYPE,
    METHOD,
    FIELD,
    CONSTRUCTOR
}
